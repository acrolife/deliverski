Commande {{#eq "shipping" transaction.protected-data.deliveryMethod}} en route{{else}}récupérée{{/eq}} - Your order for {{transaction.listing.title}} was marked as {{#eq "shipping" transaction.protected-data.deliveryMethod}}out for delivery{{else}}picked up{{/eq}}